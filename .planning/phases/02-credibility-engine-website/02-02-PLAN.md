---
phase: 02-credibility-engine-website
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/solutions/page.tsx
  - app/consulting/page.tsx
  - next.config.mjs
  - components/layout/TubelightNav.tsx
  - components/layout/Navbar.tsx
autonomous: true
requirements: [WEB-02, WEB-06]

must_haves:
  truths:
    - "/solutions page renders with 3-tier offer ladder (Audit, Sprint, Transformation) and zero pricing displayed"
    - "'Book a Strategy Call' CTA is prominent on /solutions page"
    - "'How We Work' 5-step process section exists on /solutions (Diagnose > Architect > Build > Deploy > Optimize)"
    - "/consulting redirects to /solutions (301)"
    - "Navigation reflects new /solutions route"
  artifacts:
    - path: "app/solutions/page.tsx"
      provides: "Solutions page with offer ladder, How We Work, and CTA"
      contains: "AI Opportunity Audit"
    - path: "next.config.mjs"
      provides: "Redirect rules for /consulting and /services to /solutions"
      contains: "redirects"
  key_links:
    - from: "app/solutions/page.tsx"
      to: "Phase 1 deliverables"
      via: "Offer tier content from scope documents"
      pattern: "AI Opportunity Audit|AI Operations Sprint|AI Transformation Program"
    - from: "components/layout/TubelightNav.tsx"
      to: "/solutions"
      via: "Navigation link"
      pattern: "solutions"
    - from: "next.config.mjs"
      to: "/solutions"
      via: "301 redirect from /consulting"
      pattern: "consulting.*solutions"
---

<objective>
Create the /solutions page with the 3-tier offer ladder from Phase 1, a "How We Work" process section, "Book a Strategy Call" CTA, and update all navigation to reference /solutions instead of /consulting and /services.

Purpose: Give prospects a clear path from interest to action -- see the offer tiers, understand the process, and book a strategy call. No pricing displayed.
Output: New /solutions page, updated navigation, redirect rules.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credibility-engine-website/02-RESEARCH.md

Phase 1 deliverables (SOURCE CONTENT for the solutions page):
@.planning/phases/01-offer-architecture-and-positioning/deliverables/01-audit-scope.md
@.planning/phases/01-offer-architecture-and-positioning/deliverables/02-sprint-scope.md
@.planning/phases/01-offer-architecture-and-positioning/deliverables/03-transformation-scope.md
@.planning/phases/01-offer-architecture-and-positioning/deliverables/11-team-narrative.md

Reference the existing consulting page for layout patterns:
@app/consulting/page.tsx

<interfaces>
<!-- Existing page and component patterns to follow -->

From app/consulting/page.tsx:
- Uses Section, Container, SectionLabel components
- Has hero section with headline + subhead
- Has service category cards
- Has process steps section
- Has FAQ accordion
- Has CTA section at bottom
- This page's LAYOUT PATTERN should be reused for /solutions, but all CONTENT must change

From lib/constants.ts:
```typescript
export const navLinks = [
  { href: '/services', label: 'Services' },
  { href: '/blog', label: 'Blog' },
  { href: '/case-studies', label: 'Case Studies' },
  { href: '/ai-builders-club', label: 'AI Builders Club' },
  { href: '/consulting', label: 'Consulting' },
  { href: '/about', label: 'About' },
]
```
NOTE: Plan 01 is NOT modifying navLinks. This plan owns navLinks updates.

From components/layout/TubelightNav.tsx:
- Has navItems array (may duplicate or extend navLinks)
- Has mobileNavLinks
- May have mega menu sections for Services and Consulting
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /solutions page with offer ladder, How We Work, and CTA</name>
  <files>app/solutions/page.tsx</files>
  <action>
  Read `app/consulting/page.tsx` first to understand existing page patterns (Section, Container, SectionLabel usage, layout structure, SEO metadata pattern).

  Read Phase 1 deliverables for content:
  - `01-audit-scope.md` for AI Opportunity Audit content
  - `02-sprint-scope.md` for AI Operations Sprint content
  - `03-transformation-scope.md` for AI Transformation Program content

  Create `app/solutions/page.tsx` with these sections:

  **1. SEO Metadata:**
  - Title: "Solutions | Saksham Solanki -- AI Systems That Deliver ROI"
  - Description: Premium positioning, no pricing, no "affordable"
  - Schema markup (if consulting page had it, update to match new offer names)

  **2. Hero Section:**
  - Headline: "AI Systems Built for Business Impact" (or similar premium, outcome-focused)
  - Subhead: Brief statement about the 3-tier approach -- diagnose, build, transform
  - NO pricing anywhere

  **3. Offer Ladder (3 cards):**
  Use dark cards with mint accents following existing card patterns.

  Card 1 -- AI Opportunity Audit:
  - Label: "ENTRY POINT" (mint, ALL CAPS, small)
  - Title: "AI Opportunity Audit"
  - Tagline: "Find the $500K you're leaving on the table."
  - 2-3 sentence description from audit scope document
  - Key deliverables list (3-4 items): AI Opportunity Report, Executive Summary, Implementation Roadmap, ROI Projections
  - CTA: "Book Your Audit" linking to #contact section or Calendly
  - NO PRICING -- this is a locked decision

  Card 2 -- AI Operations Sprint:
  - Label: "CORE OFFER" (mint)
  - Title: "AI Operations Sprint"
  - Tagline: "Deployed systems, measurable results, 60 days."
  - Description from sprint scope
  - Key deliverables: Production AI Systems, Integration with Existing Stack, Team Training, 30-Day Support
  - CTA: "Start Your Sprint"

  Card 3 -- AI Transformation Program:
  - Label: "ENTERPRISE" (mint)
  - Title: "AI Transformation Program"
  - Tagline: "Full-stack operational overhaul with P&L impact."
  - Description from transformation scope
  - Key deliverables: Multi-System Deployment, Change Management, Executive Dashboard, Ongoing Optimization
  - CTA: "Discuss Your Transformation"

  **4. "How We Work" Section (WEB-06):**
  - Section label: "HOW WE WORK" (mint, ALL CAPS)
  - 5-step process with icons (use Lucide React):
    1. Diagnose -- "We map your operations and identify AI opportunities with the highest ROI."
    2. Architect -- "We design the system architecture, data flows, and integration points."
    3. Build -- "Our engineering team builds and tests production-grade AI systems."
    4. Deploy -- "We deploy into your environment with zero disruption to operations."
    5. Optimize -- "We monitor, measure, and optimize for continuous improvement."
  - Use horizontal layout on desktop (5 columns with connecting lines/arrows), stack on mobile

  **5. Social Proof Section:**
  - Client logo marquee (reuse existing LogoMarquee component)
  - Can add the same 4 metrics from Plan 01 here too if layout allows

  **6. CTA Section (bottom):**
  - Headline: "Ready to See What AI Can Do for Your Operations?"
  - Subhead: "Book a strategy call. We'll identify the highest-impact opportunities in your business."
  - Primary CTA: "Book a Strategy Call" -- link to Calendly (from siteConfig.links.calendly) or #contact anchor
  - Add `id="contact"` to this section for in-page linking
  - This section will later have the pre-qualification form embedded (Plan 02-04)

  **Style requirements:**
  - Dark mode default, Electric Mint accents
  - Satoshi font, 8px grid spacing
  - Use existing Section, Container, SectionLabel, Button components
  - Reuse card patterns from consulting page
  - Framer Motion fade-in animations (match existing page patterns)
  - NO pricing displayed anywhere -- this is NON-NEGOTIABLE
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SakshamSolanki.com && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>/solutions page renders with 3-tier offer ladder (Audit, Sprint, Transformation), "How We Work" 5-step process, "Book a Strategy Call" CTA, zero pricing displayed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation, add redirects, clean up consulting page</name>
  <files>lib/constants.ts, components/layout/TubelightNav.tsx, components/layout/Navbar.tsx, next.config.mjs, app/consulting/page.tsx</files>
  <action>
  Read all files fully before editing.

  **In `lib/constants.ts`:**
  1. Update `navLinks` array:
     - Replace `{ href: '/services', label: 'Services' }` with `{ href: '/solutions', label: 'Solutions' }`
     - Remove `{ href: '/consulting', label: 'Consulting' }` (now redirects to /solutions)
     - Keep Blog, Case Studies, AI Builders Club, About as-is
  2. Final navLinks should be: Solutions, Blog, Case Studies, AI Builders Club, About

  **In `components/layout/TubelightNav.tsx`:**
  1. Read the file to find `navItems` and `mobileNavLinks` arrays
  2. Update them to match the new navLinks (replace /services and /consulting with /solutions)
  3. If there are mega menu sub-items for Services or Consulting, remove them. /solutions is a single page, not a category with sub-pages. Use a simple nav link.

  **In `components/layout/Navbar.tsx`:**
  1. Check if it has its own nav items separate from constants. If so, update to match.

  **In `next.config.mjs`:**
  1. Add permanent redirects:
     ```javascript
     async redirects() {
       return [
         {
           source: '/consulting',
           destination: '/solutions',
           permanent: true,
         },
         {
           source: '/services',
           destination: '/solutions',
           permanent: true,
         },
       ]
     }
     ```
  2. If `redirects()` already exists in the config, merge these entries.

  **In `app/consulting/page.tsx`:**
  1. The redirect in next.config.mjs handles external/SEO traffic, but the page file can remain as a fallback. Either:
     a. Replace content with a simple redirect component (`redirect('/solutions')` from next/navigation), OR
     b. Leave the file but know the redirect catches requests before they reach it
  2. Option (a) is cleaner -- replace the entire page with a server-side redirect.

  **Verify no broken internal links:**
  After edits, grep for remaining `/consulting` and `/services` links across the codebase (excluding the redirect config itself and .planning files). Fix any found.
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SakshamSolanki.com && npm run build 2>&1 | tail -5 && echo "---LINK CHECK---" && grep -rn "href.*\/consulting\|href.*\/services" app/ components/ lib/ --include="*.tsx" --include="*.ts" | grep -v "next.config\|\.planning\|redirect\|destination" || echo "CLEAN: No old links found"</automated>
  </verify>
  <done>Navigation shows "Solutions" instead of "Services" and "Consulting". /consulting and /services both 301 redirect to /solutions. No broken internal links to old routes. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `curl -I localhost:3000/consulting` returns 301 redirect to /solutions (when dev server running)
3. /solutions page shows 3-tier offer ladder with zero pricing
4. "How We Work" section shows 5-step process
5. Navigation bar shows "Solutions" link
6. No references to /consulting or /services remain in component/page code
</verification>

<success_criteria>
- /solutions page exists with Audit, Sprint, Transformation offer cards and zero pricing
- "How We Work" section shows Diagnose > Architect > Build > Deploy > Optimize
- "Book a Strategy Call" CTA is prominent on the page
- /consulting and /services redirect to /solutions (301 permanent)
- Navigation updated across all nav components
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-credibility-engine-website/02-02-SUMMARY.md`
</output>
