---
phase: 02-credibility-engine-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/hero-scroll.tsx
  - app/page.tsx
  - lib/constants.ts
  - components/layout/Footer.tsx
  - app/services/page.tsx
autonomous: true
requirements: [WEB-01, WEB-04, WEB-05]

must_haves:
  truths:
    - "Homepage hero communicates premium AI systems partner positioning with outcome-focused headline"
    - "Zero small-ticket signals remain anywhere on site -- no hourly pricing, no 'affordable', no generic service lists, no '11-200 employees'"
    - "Social proof metrics bar (systems deployed, industries served, hours saved, ROI) is visible on homepage"
    - "Site title, author role, footer tagline all reflect premium positioning"
  artifacts:
    - path: "components/ui/hero-scroll.tsx"
      provides: "Rewritten hero with premium headline, subhead, and CTAs"
      contains: "Book a Strategy Call"
    - path: "app/page.tsx"
      provides: "Updated homepage with premium services preview and social proof metrics"
      contains: "AI Systems Deployed"
    - path: "lib/constants.ts"
      provides: "Updated site config with premium positioning"
    - path: "components/layout/Footer.tsx"
      provides: "Updated footer tagline"
  key_links:
    - from: "components/ui/hero-scroll.tsx"
      to: "/solutions"
      via: "CTA button href"
      pattern: "href.*solutions"
    - from: "app/page.tsx"
      to: "/case-studies"
      via: "View Results secondary CTA"
      pattern: "href.*case-studies"
---

<objective>
Rewrite the homepage hero for premium AI systems partner positioning, clean all small-ticket anti-features across the site, and add a social proof metrics bar to the homepage.

Purpose: When a prospect lands on sakshamsolanki.com, they immediately see premium positioning -- outcome-focused value, social proof, and zero signals of a cheap dev shop.
Output: Updated hero copy, cleaned site-wide anti-features, social proof metrics on homepage.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credibility-engine-website/02-RESEARCH.md

Phase 1 deliverables (for messaging reference):
@.planning/phases/01-offer-architecture-and-positioning/deliverables/01-audit-scope.md
@.planning/phases/01-offer-architecture-and-positioning/deliverables/11-team-narrative.md

<interfaces>
<!-- Key files to modify. Read these before editing. -->

From lib/constants.ts:
```typescript
export const siteConfig = {
  title: 'Saksham Solanki | AI Automation Systems Architect',  // UPDATE
  description: 'I build AI systems that work...',  // UPDATE
  author: { role: 'AI Automation Systems Architect' },  // UPDATE
}
export const navLinks = [
  { href: '/services', label: 'Services' },  // Will be updated in Plan 02
  { href: '/consulting', label: 'Consulting' },  // Will be updated in Plan 02
]
```

From components/ui/hero-scroll.tsx:
- Contains hero headline "I build AI systems that work."
- Caption "AI Automation Systems Architect"
- CTAs: "Join AI Builders Club" + "Book a Call"
- Proof stats: "50+ AI systems deployed" + "$2M+ in client ROI"
- Uses Spline 3D scene, Spotlight effect, InteractiveHoverButton -- keep all of these

From components/layout/Footer.tsx:
- Contains tagline "Building production-grade AI infrastructure for B2B companies"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite homepage hero and services section for premium positioning</name>
  <files>components/ui/hero-scroll.tsx, app/page.tsx</files>
  <action>
  Read `components/ui/hero-scroll.tsx` and `app/page.tsx` fully before editing.

  In `hero-scroll.tsx`:
  1. Update caption from "AI Automation Systems Architect" to "AI Systems Partner"
  2. Rewrite headline. Replace "I build AI systems that work." with something outcome-focused and premium. Example: "AI systems that deliver measurable ROI." -- but use judgment for what reads best. Must NOT contain: "affordable", "cheap", hourly rates, "11-200 employees", or any banned words (leverage, synergy, game-changer, revolutionary, cutting-edge, disruptive, robust, seamlessly, transformative).
  3. Rewrite subhead to focus on business outcomes and premium positioning. Reference track record (50+ systems, documented ROI). Example: "We design, build, and deploy AI infrastructure that reduces costs and accelerates operations. 50+ production systems. $2M+ in documented results." Adjust for operator voice.
  4. Change primary CTA from "Join AI Builders Club" to "Book a Strategy Call" linking to `/solutions#contact` (or Calendly link from constants as fallback)
  5. Change secondary CTA from "Book a Call" to "View Results" linking to `/case-studies`
  6. Keep all existing component structure (Spline 3D, Spotlight, InteractiveHoverButton, animations)

  In `app/page.tsx`:
  1. Update meta description -- remove "11-200 employees" or "B2B companies" narrow targeting. Use broader premium positioning.
  2. Find the services section (currently shows 5 generic cards: AI Solutions, AI for Marketing, Custom Software, Product & Startups, Marketing). Replace with a premium 3-card preview of the offer ladder:
     - Card 1: "AI Opportunity Audit" -- "Find the $500K you're leaving on the table."
     - Card 2: "AI Operations Sprint" -- "Deployed systems, measurable results, 60 days."
     - Card 3: "AI Transformation Program" -- "Full-stack operational overhaul with P&L impact."
     Each card links to `/solutions`. Use existing card component patterns from the page.
  3. Add a social proof metrics bar section on the homepage (can be placed after hero or after services preview). Use the existing MetricsBar component pattern or create an inline version with 4 stats:
     - "50+" / "AI Systems Deployed"
     - "16+" / "Industries Served"
     - "10,000+" / "Hours of Manual Work Eliminated"
     - "$2M+" / "Documented Client ROI"
     Style with the existing design system (dark bg, Electric Mint accent for values, Satoshi font).
  4. If there is a consulting section on the homepage with 4 generic cards, replace or remove it. Point any CTAs to `/solutions`.
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SakshamSolanki.com && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Homepage hero shows premium outcome-focused headline with "Book a Strategy Call" primary CTA and "View Results" secondary CTA. Services section shows 3-tier offer preview (not 5 generic categories). Social proof metrics bar visible on homepage. No "11-200 employees" in meta description.</done>
</task>

<task type="auto">
  <name>Task 2: Anti-feature cleanup across site globals and services page</name>
  <files>lib/constants.ts, components/layout/Footer.tsx, app/services/page.tsx</files>
  <action>
  Read each file fully before editing.

  In `lib/constants.ts`:
  1. Update `siteConfig.title` from "AI Automation Systems Architect" to "AI Systems Partner" (or similar premium title)
  2. Update `siteConfig.description` to remove any generic/small-ticket language. Focus on premium AI consulting positioning.
  3. Update `siteConfig.author.role` from "AI Automation Systems Architect" to "AI Systems Partner"
  4. Do NOT modify `navLinks` here -- Plan 02 handles navigation changes to avoid file conflicts.

  In `components/layout/Footer.tsx`:
  1. Update tagline from "Building production-grade AI infrastructure for B2B companies" to something premium-positioned. Example: "Designing and deploying AI systems that deliver measurable business results." Use operator voice, no banned words.

  In `app/services/page.tsx`:
  1. This page lists 5 broad service categories (generalist dev shop positioning). It needs to either:
     a. Be gutted and turned into a redirect to `/solutions` (preferred -- add a simple redirect component or page that redirects client-side), OR
     b. Have its content significantly reduced to just point to `/solutions`
  2. The redirect in `next.config.mjs` will be handled by Plan 02. For now, make the page content redirect-friendly or minimal.

  After all edits, run a grep to verify no remaining anti-features:
  ```
  grep -r "affordable\|\$2,000\|\$15,000\|\$3,000/month\|hourly rate\|11-200\|11–200" app/ components/ lib/ --include="*.tsx" --include="*.ts"
  ```
  Fix any remaining hits (excluding files owned by Plan 02 like consulting/page.tsx).
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SakshamSolanki.com && npm run build 2>&1 | tail -5 && echo "---ANTI-FEATURE CHECK---" && grep -rn "affordable\|\\\$2,000\|\\\$15,000\|\\\$3,000/month\|hourly rate\|11-200\|11–200" app/page.tsx app/services/ components/ lib/constants.ts 2>&1 || echo "CLEAN: No anti-features found"</automated>
  </verify>
  <done>Site title, description, author role all reflect premium positioning. Footer tagline updated. Services page redirects to or references /solutions. Grep confirms zero anti-features in files owned by this plan.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `grep -r "affordable\|\$2,000\|\$15,000\|\$3,000/month\|hourly rate\|11-200\|11–200" app/page.tsx app/services/ components/ lib/constants.ts` returns no matches
3. Homepage hero shows premium headline with "Book a Strategy Call" CTA
4. Homepage has social proof metrics bar with 4 stats
5. Services section shows 3-tier offer preview, not 5 generic categories
</verification>

<success_criteria>
- Homepage hero communicates outcome-focused premium value with zero small-ticket signals
- Social proof metrics bar visible on homepage with real aggregate numbers
- All global config (title, description, footer) reflects premium AI systems partner positioning
- Services page no longer shows generalist dev shop positioning
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-credibility-engine-website/02-01-SUMMARY.md`
</output>
